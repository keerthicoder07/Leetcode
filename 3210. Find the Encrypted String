class Solution {
    public String getEncryptedString(String s, int k) {
        char str[]=s.toCharArray();
        StringBuilder s1=new StringBuilder();
        for(int i=0;i<s.length();i++)
        {
            s1.append(str[(i+k)%s.length()]);
        }
        return s1.toString();
    }
}
